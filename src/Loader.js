import React from "react";

export default () => (
  <div>
    <div
      style={{ display: "flex", justifyContent: "center", margin: ".5rem auto" }}
      className="lds-dual-ring"
    />
  </div>
);
